def parrot(string="Squawk!"):
    """
    Accepts a string argument, prints it, and returns it.
    
    Parameters:
        string (str): The string to be printed and returned. Defaults to "Squawk!" if not provided.
        
    Returns:
        str: The input string.
    """
    print(string)
    return string
